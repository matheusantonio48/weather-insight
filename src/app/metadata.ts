import type { Metadata } from 'next';

const metadata: Metadata = {
  title: 'Weather Insight',
  description: 'Get detailed and accurate weather forecasts',
  icons: [],
};

export default metadata;
